{
    "type": "object",
    "properties": {
      "deploy": {
        "type": "object",
        "properties": {
          "ansible": {
            "type": "object",
            "properties": {
              "playbook": {
                "type": "object",
                "properties": {
                  "tasks": {
                    "type": "array",
                    "items": {
                      "oneOf": [
                        {
                          "type": "object",
                          "properties": {
                            "docker/install": {
                              
                            }
                          }
                        }
                      ]
                    },
                    "properties": {
                      "docker": {
                        "type": "object",
                        "properties": {
                          "retries": {
                            "description": "Number of retries",
                            "type": "integer",
                            "minimum": 1,
                            "maximum": 10
                          },
                          "delay": {
                            "description": "Delay in seconds between installation attempts",
                            "type": "integer",
                            "minimum": 5,
                            "maximum": 120
                          }
                        },
                        "required": ["retries", "delay"]
                      }
                    },
                    "required": ["docker"]
                  }
                },
            },
            "required": ["install"]
          }
        },
        "required": ["ansible"]
      }
    },
    "required": ["deploy"]
  }
  